<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="5" author="arman">
        <!-- Add user_id to orders table -->
        <addColumn tableName="orders">
            <column name="user_id" type="BIGINT">
                <constraints foreignKeyName="fk_orders_user"
                             referencedTableName="users" referencedColumnNames="id"/>
            </column>
        </addColumn>

        <!-- Add selected_shop_id to users table -->
        <addColumn tableName="users">
            <column name="selected_shop_id" type="BIGINT">
                <constraints foreignKeyName="fk_users_selected_shop"
                             referencedTableName="shops" referencedColumnNames="id"/>
            </column>
        </addColumn>
    </changeSet>

</databaseChangeLog>
